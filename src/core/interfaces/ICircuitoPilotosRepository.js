export default class ICircuitoPilotosRepository {
    async obtenerPilotosPorCircuito(circuitoId) { // eslint-disable-line no-unused-vars
        throw new Error('Método obtenerPilotosPorCircuito debe ser implementado');
    }

    async registrarPilotoEnCircuito(circuitoId, pilotoId) { // eslint-disable-line no-unused-vars
        throw new Error('Método registrarPilotoEnCircuito debe ser implementado');
    }

    async eliminarPilotoDeCircuito(circuitoId, pilotoId) { // eslint-disable-line no-unused-vars
        throw new Error('Método eliminarPilotoDeCircuito debe ser implementado');
    }
}