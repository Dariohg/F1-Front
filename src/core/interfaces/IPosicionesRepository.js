export default class IPosicionesRepository {
    async actualizarPosicion(circuitoId, conductorId, posicion) { // eslint-disable-line no-unused-vars
        throw new Error('Método actualizarPosicion debe ser implementado');
    }

    async obtenerPosiciones(circuitoId) { // eslint-disable-line no-unused-vars
        throw new Error('Método obtenerPosiciones debe ser implementado');
    }
}