export class TiempoVuelta {
    constructor({ conductor_id, numero_vuelta, tiempo }) {
        this.conductor_id = conductor_id;
        this.numero_vuelta = numero_vuelta;
        this.tiempo = tiempo;
    }
}